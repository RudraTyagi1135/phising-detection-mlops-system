name: cd

on:
  workflow_run:
    workflows:
      - ci
    types:
      - completed

permissions:
  contents: read

jobs:
  deploy:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'

    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login To Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build -t rudratyagi1/phishguard-ai:latest .

      - name: Push Docker Image
        run: |
          docker push rudratyagi1/phishguard-ai:latest

      # - name: Deploy To EC2
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ secrets.EC2_HOST }}
      #     username: ${{ secrets.EC2_USERNAME }}
      #     key: ${{ secrets.EC2_SSH_KEY }}
      #     script: |
      #       docker pull rudratyagi1/phishguard-ai:latest

      #       docker stop phishguard-ai || true
      #       docker rm phishguard-ai || true

      #       docker run -d \
      #         --name phishguard-ai \
      #         --restart unless-stopped \
      #         -p 8000:8000 \
      #         rudratyagi1/phishguard-ai:latest

      #       docker image prune -af